extends ../components/base/layout

block variables
  - const title = "Animations" //- на страницах кроме главной используйте О нас — ProjectName
  - const wrapperMods = ""
  - const hasHeader = true
  - const headerMods = ""
  - const hasFooter = false
  - const hasLoader = true
  - const footerMods = ""
  - const currentUrl = "animations.html" //- для остальных страниц пишем пустую строку или индекс активного headerLinks

block main
  main
    //- на странице запрещен "голый" код
    //- все блоки вставляются из папки pug/components/blocks
    +intro()
    +section(dataSection1)(id="menu")
    +section(dataSection2)(id="loader")
    +section(dataSection3)(id="first-skreen")
    +section(dataSection4)(id="appear")
    +section(dataSection5)(id="parallax")
    +scroll-slider()(id="scroll")

block modals
